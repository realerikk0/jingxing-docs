---
title: "鉴权方式"
description: "说明 Bearer Token、控制台登录与常见鉴权错误"
sidebar_position: 4
---

# 鉴权方式

这页不讲抽象授权模型，只讲用户最常碰到的两个问题：

1. **API 到底该用什么 token**
2. **为什么我明明有 token，还是 401 或 403**

## 先说结论

- 调 API 时，默认使用 **Bearer Token**
- 控制台登录状态不等于 API 可用
- `401` 更常见于 token 本身有问题
- `403` 更常见于权限、分组、模型或来源限制问题

## API 默认怎么传

默认请求头是：

```http
Authorization: Bearer <YOUR_TOKEN>
```

如果你用的是 `curl`、SDK、客户端或自部署前端，本质上都是把这个值放到请求里。

## 什么不是 API 鉴权

很多人第一次会把下面几件事混在一起：

- 控制台登录
- API token
- 某个客户端本地缓存的 key

要分清：

- 控制台登录，是你进入网页工作台的身份
- API token，是程序、客户端和脚本真正调用模型时用的凭证

控制台能登录，不代表 API 一定通。  
API 通，也不代表你控制台里一定配对了当前那把 token。

## 最小验证方式

最稳的做法永远是先跑：

```bash
curl https://api.jingxing.uk/v1/models \
  -H "Authorization: Bearer <YOUR_TOKEN>"
```

这一步至少能验证：

- Base URL 是不是对的
- token 有没有生效
- 当前 token 大致能看到哪些模型

## 401 和 403 的区别

### 401

更常见于：

- 没带 token
- token 复制不完整
- token 过期
- token 已停用

### 403

更常见于：

- 当前分组不开放该模型
- token 限制了模型范围
- 开了 IP / 来源限制
- 当前操作本身没权限

所以：

- `401` 先查 token
- `403` 先查策略

## 令牌应该怎么用才安全

最推荐的方式不是“一个大 token 到处复用”，而是：

- 一个客户端一个 token
- 一个服务一个 token
- 一个环境一个 token

这样做的好处是：

- 泄露时影响面更小
- 审计更清晰
- 排障更快

## 令牌泄露后怎么办

建议顺序：

1. 先停用或轮换可疑 token
2. 再看最近调用记录
3. 再决定是否要收紧模型权限、分组或来源限制

不要在已经怀疑泄露的情况下还继续长期保留旧 token。

## 令牌轮换建议

按用途来看更稳：

- 临时测试：短有效期
- 桌面客户端：定期轮换
- 生产服务：独立 token + 轮换流程

如果你已经把 token 发给了不该长期保留的地方，那就不要犹豫，直接换。

## 模型限制和来源限制什么时候会生效

### 模型限制

当 token 只被允许访问部分模型时，客户端即使拿到了 token，也不代表它能调用所有模型。

### 来源限制

如果平台或 token 配了 IP / 来源限制，那么：

- 在允许来源内：正常
- 在不允许来源内：通常会报 `403`

这类问题最容易在：

- 本地开发切网络
- 办公室 / 家里切换
- 部署环境变更

时出现。

## 常见问题

### 我明明复制了 token，为什么还是 401

优先检查：

- token 前后有没有多空格
- 客户端是不是还缓存旧值
- 有没有漏掉 `Bearer `

### 我能看到模型，但一调用就 403

优先检查：

- 当前模型是否真的在 token 权限范围内
- 当前分组是否允许该模型完整调用
- 当前来源是否被限制

### 为什么网页能进，API 却不行

因为这根本就是两套路径：

- 网页依赖登录会话
- API 依赖 Bearer Token

## 推荐动作

如果你现在正在排查鉴权问题，最省时间的顺序是：

1. 先确认 Base URL
2. 先确认 Bearer Token
3. 跑 `GET /models`
4. 再发最小 `chat/completions`
5. 最后再回具体客户端排查

## 下一步

- 想看 401 / 403 / 429 一页速查：前往[HTTP 状态码总览](/docs/faq/http-status-codes)
- 想看 token 怎么建和怎么轮换：前往[令牌管理](/docs/platform/token-management)
