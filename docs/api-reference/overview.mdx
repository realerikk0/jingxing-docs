---
title: "API 概览"
description: "按能力说明井陉中转 API 的主要接口路径"
sidebar_position: 6
---

# API 概览

井陉 API 以 OpenAI 兼容接口为主，当前按能力划分为聊天、Responses、图片和模型列表查询。

除特殊说明外，请求地址均以以下基础地址为前缀：

```txt
https://api.jingxing.uk
```

## 聊天

聊天接口用于多轮对话、文本生成、代码生成、信息抽取、内容改写、结构化回答等场景。大多数客户端和 OpenAI 兼容 SDK 会优先使用该接口。

对应路径：

- `POST /v1/chat/completions`

常见用途：

- 普通问答
- 多轮上下文对话
- 代码解释与生成
- 文档总结
- JSON 或结构化内容生成

## Responses

Responses 接口用于 OpenAI Responses 风格的模型调用，适合需要更统一响应结构、工具调用、推理流程或更现代 OpenAI 接口形态的场景。

对应路径：

- `POST /v1/responses`
- `POST /v1/responses/compact`

常见用途：

- 新版 OpenAI 风格应用接入
- 多轮任务处理
- 工具调用与复杂推理流程
- 长对话压缩与上下文整理

## 图片

图片接口用于生成图片或基于已有图片进行编辑。适合产品配图、创意素材、视觉草图、封面图和图像修改等场景。

对应路径：

- `POST /v1/images/generations`
- `POST /v1/images/edits`

常见用途：

- 文生图
- 图片局部编辑
- 风格化生成
- 视觉素材批量生成
- 营销图、封面图、示意图制作

## 模型列表查询

模型列表查询用于查看当前令牌、分组和权限下可见的模型。首次接入、客户端配置、模型不可用排查时，建议先调用该接口。

对应路径：

- `GET /v1/models`
- `GET /v1beta/models`

常见用途：

- 验证 API Key 是否有效
- 查看当前令牌可用模型
- 确认可用分组和模型范围
- 排查模型名称填写错误
- 在客户端中同步模型列表
